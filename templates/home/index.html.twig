{% extends 'layout.html.twig' %}

{% block title %}The Movie Collection{% endblock %}

{% block body %}
    <div class="home_background">
        <div class="container-fluid">
            <div class="row justify-content-center">
            {{ form_start(form) }}
            {{ form_errors(form) }}
            {{ form_row(form.search) }}

            {{ form_row(form.submit) }}
            {{ form_end(form) }}
            </div>
        </div>
    </div>
{% endblock %}
